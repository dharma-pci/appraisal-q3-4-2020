<odoo>
    <record id="delivery_order_wizard_view_form" model="ir.ui.view">
        <field name="name">delivery.order.wizard.view.form</field>
        <field name="model">delivery.order.wizard</field>
        <field name="arch" type="xml">
            <form create="false" edit="false">
                <sheet>

                    <group>
                        <field name="sale_order_id" invisible="1"/>
                    </group>
                    <label for="order_line"/>
                    <field
                        name="order_line"
                        widget="section_and_note_one2many"
                        mode="tree">
                        <tree string="Delivery Order" create="false" edit="false">
                            <field name="company_id" invisible="1"/>
                            <field name="product_id" widget="product_configurator"/>
                            <field name="name"/>
                            <field name="product_uom_qty"/>
                            <field name="price_unit"/>
                        </tree>
                    </field>

                    <footer>
                        <button name="generate_delivery_order" string="Generate" type="object" class="oe_highlight"/>
                        <button string="Cancel" class="btn btn-default" special="cancel"/>
                    </footer>

                </sheet>
            </form>
        </field>
    </record>
</odoo>